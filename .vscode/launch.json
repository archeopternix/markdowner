{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run cmd/sample",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${workspaceFolder}/cmd/sample",
      "cwd": "${workspaceFolder}"
    }
  ]
}